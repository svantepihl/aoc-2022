import { getSolutionPartOne, getSolutionPartTwo } from ".";

describe("getSolutionPartOne", () => {
  it("should throw", () => {
    expect(() => getSolutionPartOne("")).toThrow();
  });
});

describe("getSolutionPartTwo", () => {
  it("should throw", () => {
    expect(() => getSolutionPartTwo("")).toThrow();
  });
});
